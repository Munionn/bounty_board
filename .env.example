# Postgres (matches docker-compose.yml)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/bounty_board

# Axum server
HOST=0.0.0.0
PORT=3000

# Solana (optional — for later backend/RPC use)
SOLANA_RPC_URL=http://127.0.0.1:8899
